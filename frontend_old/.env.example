# Backend API base URL (no trailing slash)
# Development: leave empty - Vite proxy will forward /api to http://localhost:8000
# Production (Vercel): set to your Railway backend URL
VITE_API_BASE_URL=https://your-orca-backend.railway.app
